#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

void solve() {
	int q, a0, c0;
	cin >> q >> a0 >> c0;

	while (q--) {
		int t;
		cin >> t;
		if (t == 1) {
			int p, a, c;
			cin >> p >> a >> c;

		} else if (t == 2) {
			int v, w;
			cin >> v >> w;

		}
	}
}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	
	int t{1};
	cin >> t;
	while (t--) solve();

	return 0;
}
